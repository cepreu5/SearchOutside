{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension (ts-node)",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "env": {
        "TS_NODE_TRANSPILE_ONLY": "true"
      },
      "outFiles": []
    }
  ]
}
